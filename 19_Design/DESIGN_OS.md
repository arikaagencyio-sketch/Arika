# Design — Department OS

**Department:** Design (19)
**Position in flow:** Horizontal support layer — the agency's creative production studio. Sits downstream of Branding (12) (identity/strategy) and Content (04) (briefs/narrative), converting both into finished assets; feeds Marketing (03)'s distribution and, once packaged as a client offer, Offer (02). Reports into Agency Governance (00).
**Mandate:** Own the agency's creative asset production — the asset library, the AI/production engine, and Canva as the creative-assembly layer — for Arika Agency's own content and campaigns first, and (once packaged) as a service for client/sector design work.
**Owner:** Mary Thuo

> See [`GLOBAL_OS.md`](../GLOBAL_OS.md) for how this file fits into the whole system. Read that first if you haven't.

---

## 1. Identity

Design is the newly created department that turns Branding's identity system and Content's briefs into finished creative assets. It exists because Branding (12)'s mandate is brand strategy and identity — narrative, positioning, visual cognition as a specification — not production execution; nothing in this repo before this owned the actual making of assets. Design is deliberately the production studio, not a second identity department: it consumes Branding's confirmed identity (palette, type, logo, motion doctrine) as its constitution and produces storyboards, images, video, motion graphics, and finished Canva assemblies against it.

Explicit dual mandate, same pattern as Automation (16): serves every internal department first (Content's content-house pipeline is its first real workload), and becomes a client-facing capability later once packaged as a sellable offer (cross-reference Offer, 02 — not built here).

Downstream of Design sits the new **Experience Engineering (20)** department (added 2026-07-03) — it consumes Design's curated assets and motion primitives to build whole interactive experiences (websites, presentations, product launches, interactive reports); Design remains the asset-production layer, Experience Engineering the experience-assembly layer above it, same split pattern as Branding→Design. See `20_Experience_Engineering/EXPERIENCE_ENGINEERING_OS.md`.

**Naming-collision note (2026-07-09):** the owner's "Design Department" web-build plugin (a brief→build-ready-spec discipline for interactive sites, stack Next.js/Tailwind/GSAP/Lenis/Spline) was aligned into **Experience Engineering (20)**, *not* this department — despite the shared word "Design," it governs interactive *web builds*, which is EE (20)'s mandate, whereas Design (19) owns *asset/media* production (images, video, motion primitives, Canva assemblies). Full spec: `20_Experience_Engineering/EXPERIENCE_SPEC_SYSTEM.md`. The two departments meet only at the token layer (that system's Style Tokens station = this department's `DESIGN_LANGUAGE_SYSTEM.md` token surface), where both consume Branding (12)'s Brand Genome as the single source of truth — neither re-invents the palette.

## 2. Status

**Newly created department (2026-07-01)**, seeded directly from an owner planning session rather than promoted from an existing Offer-catalog division (contrast with Audits & Diagnostics/Consulting & Advisory/Automation/AI Enablement, which were promotions). No raw draft archive exists for this department. The owner supplied directional brand-visual material (moodboard imagery: a gold-on-navy palette direction named Deep Navy/Charcoal Gray/Silver Mist/Accent Gold, and a display/body typeface direction — Space Grotesk/Satoshi/Neue Montreal display, Inter/Manrope body) plus a detailed production-architecture vision (asset libraries, a multi-stage AI production engine, Canva as assembly not generation, campaign-first folder structure, a "Creative Digital Twin" environment metaphor, storyboard-before-generation, and a Design Language MCP/API + skill library). That vision is the seed content for this file, captured in full in `DESIGN_INSPIRATION.md` — same "captured before being built" status Automation's founding offers had on day one.

**Only one capability below is actually live**: Canva, confirmed connected (MCP tools present in this environment, premium tier). Everything else — the Asset Library, the Production Engine, the Design Language MCP/API — is documented architecture, not built capability, same distinction Tech Stack (13) draws for its own placeholder rows.

## 3. Capability Registry

| Capability | Description | Status |
|---|---|---|
| Creative Assembly (Canva) | Where approved assets get assembled into finished, publishable creative — carousels, video, presentations, ads — organized campaign-first (§10) rather than platform-first | **Confirmed real and connected** — MCP tools live in this environment, premium tier confirmed by owner |
| Asset Library | Reusable production assets grouped as sub-libraries: Environment (offices, war rooms, innovation labs, podcast studio, AI control rooms), Character (recurring roles — CEO/Founder/Consultant/CMO/Sales Leader — with consistent wardrobe/lighting/age), Prop (recurring objects — glass dashboards, premium notebooks, architectural desks, holographic displays), Motion (reusable motion primitives — Executive Reveal, Data Pulse, Light Sweep, Camera Paths, Transitions), Sound (premium UI clicks, cinematic drones, ambient office beds) | Not started — structure-only, no content populated yet |
| Production Engine | AI generation/enhancement chain feeding Canva rather than replacing it: Story → Image → Video → Voice → Animation → Music → Enhancement/Upscale → Assembly. Canva receives polished assets, not raw AI output. Prompt construction for this chain is intended to eventually be standardized by Experience Engineering (20)'s proposed structured-prompting layer, not built yet — see `20_Experience_Engineering/PROMPTING_SYSTEM.md` | **Vendor confirmed 2026-07-03: OpenArt** (`https://mcp.openart.ai/mcp`, 100+ models incl. GPT Image, Nano Banana Pro, Sora 2, Kling 3.0, Seedance; image + video + inpainting + character consistency + upscaling in one platform). Considered alongside imagine.art (comparable overlapping capability) and Higgsfield (never more than a maybe) — owner chose OpenArt only, not both. MCP connection setup in progress, not yet confirmed authenticated. **2026-07-04:** owner separately named flair.ai (likely AI branded product/scene photography) in a raw tool list — not evaluated against OpenArt nor adopted; OpenArt remains the confirmed vendor. (getlayers.ai, named in the same list, turned out on confirmation to be a prompt/template library rather than a generation engine — moved to `DESIGN_INSPIRATION.md`'s Reference Galleries list instead.) Full triage: `DESIGN_INSPIRATION.md`. **2026-07-07: KIE.ai added as a second, direct-API path** for the same two capability slots — Nano Banana Pro (image) and Seedance (video) — via a real code connector (`19_Design/design-plugin`, see §13), for use when OpenArt's own credit pool is exhausted or when programmatic (non-MCP) access is needed. Not a replacement for OpenArt's broader model catalog. **Verified real and connected same day** via a live `getCredits()` call (free, no generation credits spent) — real account, 80 credits available. |
| Claude Design (visual generation) | Anthropic's own prototype/visual-generation product (Opus 4.7-powered, research preview, included in Claude Pro/Max/Team/Enterprise) — describe a visual (deck, one-pager, prototype), Claude generates and refines it, applying a consistent design system where available. Exports directly to Canva, fully editable once there | **Confirmed available 2026-07-03** — no separate MCP/account needed, included in the owner's existing Claude Pro plan. Feeds the same Canva assembly layer as the rest of the Production Engine. |
| Design Language System (MCP/API + Skill Library) | A queryable design language (tokens, motion curves, transition timing, UI/UX component patterns) plus a generalized production-skill catalog, usable for Arika's own work and future client/sector design engagements — full spec in `DESIGN_LANGUAGE_SYSTEM.md` | Proposed, not built — see §6 |
| Inspiration Library | Curated reference/moodboard archive capturing the visual direction supplied so far, now including a structured **Inspiration Brackets** intake (format-first: Website/Carousel/Reel/Poster, cross-tagged by style/mood) for ongoing taste-building — explicitly a north-star reference, not a finalized spec (finalized brand-kit values live in Branding, cross-ref `12_Branding/BRANDING_OS.md` §2) | Seeded, brackets structured but empty — see `DESIGN_INSPIRATION.md` |
| Motion/VFX/SFX/3D-5D Tool Registry | Queryable catalog of tools/MCPs for motion graphics, transitions, VFX, SFX, and 3D/5D motion, with real live links so agents can invoke them directly — full spec in `DESIGN_LANGUAGE_SYSTEM.md` §2a | Skeleton only — Remotion named by owner but not connected; 3 other categories open, no tools named yet (`OWNER_INPUT_NEEDED.md` item 48) |

## 4. Workflow Index

**Updated 2026-07-04 — each step now names the real agent that performs it**, now that Design's Agent Roster (§5) is real. This is the first time this workflow is executable step-by-step by named agents rather than a conceptual pipeline.

| Workflow | Trigger | Steps (agent → agent) | Output | Owner | Source |
|---|---|---|---|---|---|
| Creative production pipeline | Content brief approved (`04_Content/CONTENT_OS.md` §4 Asset Production Workflow reaches "Distribution Prep") | **Storyboard Generator** (produces the 7-field storyboard) → **Production Engine Coordinator** (routes through OpenArt/Claude Design: Story→Image→Video→Voice→Animation→Music→Enhancement/Upscale) → **Brand & Environment Consistency Checker** (pass/flag gate) → **Canva Assembler** (files + assembles into the real folder structure) → Publish | Finished, on-brand creative asset | Mary Thuo | This planning session; agent mapping added 2026-07-04 |
| Canva folder/campaign setup | New campaign approved (`04_Content/CONTENT_OS.md` Campaign Architecture, §10) | **Canva Assembler** creates the campaign folder under `Campaigns/` → populates `Storyboards/Generated Assets/Video/Carousel/Presentation/Thumbnail/Ads/Final` sub-folders | Ready-to-fill campaign workspace in Canva | Mary Thuo | §10 below |
| Asset reuse check | Any new asset request, before generation starts | **Asset Librarian** checks the 5 sub-libraries (Environment/Character/Prop/Motion/Sound) for an existing match → if none, hands off to Storyboard Generator; if found, hands the existing asset straight to Canva Assembler | Either a reused asset (no generation needed) or confirmation a new one is genuinely required | Mary Thuo | Added 2026-07-04, alongside the real Agent Roster |
| Inspiration intake | Owner shares a new reference (link/image/description) | **Inspiration Curator** files it into the correct format bracket, tags it, and writes the "what to extract" note | One real, tagged Inspiration Bracket entry | Mary Thuo | Added 2026-07-04 |

## 5. Agent Roster

**Real, invokable — built 2026-07-04.** 6 Claude Code subagents under `.claude/agents/design-*.md`, callable via the Agent tool today, mapped to this department's real, already-documented capabilities:

| Role | Maps to | Real subagent file |
|---|---|---|
| Storyboard Generator | §10 storyboard-before-generation doctrine (Design's own 7-field template, distinct from Experience Engineering's 9-field one) | `.claude/agents/design-storyboard-generator.md` |
| Production Engine Coordinator | §3 Production Engine (OpenArt, Claude Design) | `.claude/agents/design-production-engine-coordinator.md` |
| Brand & Environment Consistency Checker | §10 Creative Digital Twin doctrine + Branding's Brand Genome | `.claude/agents/design-brand-environment-consistency-checker.md` |
| Canva Assembler | §10 real campaign-first Canva folder structure | `.claude/agents/design-canva-assembler.md` |
| Inspiration Curator | Inspiration Brackets (`DESIGN_INSPIRATION.md`) | `.claude/agents/design-inspiration-curator.md` |
| Asset Librarian | §3 Asset Library (Environment/Character/Prop/Motion/Sound sub-libraries) | `.claude/agents/design-asset-librarian.md` |

Unlike Experience Engineering (20)'s roster, this one was built directly from this file's own existing capabilities rather than reconciling external source drafts — no owner-relayed AI session was involved in defining these 6 roles.

## 6. Skill Library Index

Unlike most other departments (where this section is an empty placeholder), Design's skill library is meant to be populated with a real, reusable **production-capability catalog** — generalized (parameterized by brand/client) rather than hardcoded to Arika, so the same catalog serves internal content production and future external client/sector design work. Full specification, including the proposed Design Language MCP/API surface (design tokens, motion curves, transition timing, UI/UX component patterns) these skills should query against, lives in the sibling spec file `19_Design/DESIGN_LANGUAGE_SYSTEM.md` — mirrors how Branding's 20-agent contracts live in `bois/agents/AGENT_SYSTEM.md`, cross-referenced from `BRANDING_OS.md` §5, rather than being duplicated inline.

Proposed initial skill catalog (documented here, not yet built): Storyboard Generation, Brand Motion Application, UI/UX Pattern Application, Environment/Character Consistency Check, Asset Enhancement & Upscale, Canva Assembly.

## 7. KPI Dictionary (department-local)

| Metric | Formula | Source | Owner | Cadence | Threshold |
|---|---|---|---|---|---|
| *(placeholder — no real production has happened yet; candidate metrics once it does: asset turnaround time, on-brand QA pass rate, cross-department request fulfillment time)* | | | | | |

## 8. Decision Log

- 2026-07-01 — Design created as a new standalone department, numbered 19 (not 18 — `18_Cross_Domain_Synthesis` stays untouched as inactive reference archive) rather than folded into Branding (12), so brand strategy/identity (Branding) stays separate from asset production (Design). Confirmed by owner via explicit choice among 3 presented options.
- 2026-07-01 — Canva confirmed as the department's Creative Assembly platform — already connected (MCP tools present), premium tier. The one genuinely live capability in this department's registry as of this entry.
- 2026-07-01 — Design Language MCP/API and a real (non-placeholder) Skill Library Index confirmed as in-scope for this department, per explicit owner follow-up — not deferred to a later pass. Documented as proposed/not-built in §3, §6, and `DESIGN_LANGUAGE_SYSTEM.md`; building it is future work.
- 2026-07-03 — **Production Engine vendor decided: OpenArt.** Compared against imagine.art (near-identical model coverage/capability) and Higgsfield (never a firm decision, just a name floated) — owner chose OpenArt only, closing `00_Agency_Governance/OWNER_INPUT_NEEDED.md` tracker item 45. Also confirmed **Claude Design** (Anthropic's own visual-generation product, already included in the owner's Claude Pro plan) as an available capability that exports directly into the Canva assembly layer — no separate account/MCP needed for that one.
- 2026-07-03 — **Owner rejected the Claude-proposed brand-kit draft outright** — retracted, not to be used as a starting point (§10). Brand kit rework begins fresh; no new values decided yet in this entry.
- 2026-07-03 — **Brand Genome adopted as the real brand kit values**, sourced from an owner-run Gemini session, owner-confirmed over a competing "Design System Manual" palette from the same session. Full values: `12_Branding/BRANDING_OS.md` §2. Flagged transparently as AI-generated (Gemini), owner-reviewed — not a human designer sign-off.
- 2026-07-03 — **Motion/VFX/SFX/3D-5D Tool & MCP Registry and the Inspiration Brackets system added**, per explicit owner request for (a) a queryable, action-oriented catalog of motion-graphics/transition/VFX/SFX/3D tools with real live links, and (b) a structured way to capture design taste/direction (format-first brackets — Website/Carousel/Reel/Poster — cross-tagged by style/mood) so future generation work can cite concrete references. Both built as real, ready-to-fill structures with zero fabricated content — full detail in `DESIGN_LANGUAGE_SYSTEM.md` §2a and `DESIGN_INSPIRATION.md`. Owner confirmed via 2 clarifying questions: build the tool-registry skeleton now rather than guess tool names (Remotion registered as owner-named-but-unconnected; 3 other categories left open — `OWNER_INPUT_NEEDED.md` item 48), and organize inspiration by format-first folders with cross-cutting style tags (not format-only or tag-only).
- 2026-07-04 — **Agent Roster built for the first time**, per the same owner direction that made Experience Engineering (20)'s roster real: 6 Claude Code subagents (§5), mapped to this department's own existing capabilities (Production Engine, Creative Digital Twin, Canva folder structure, Inspiration Brackets, Asset Library) rather than reconciling external source drafts.
- 2026-07-04 — **Owner-supplied tool/inspiration list triaged**: 18 raw names (design tools, 3D/motion libraries, inspiration galleries) captured and honestly confidence-rated in `DESIGN_INSPIRATION.md`, further informing (not resolving) `OWNER_INPUT_NEEDED.md` item 48. Confidently-identified tools distilled into `DESIGN_LANGUAGE_SYSTEM.md` §2a (Khroma, Jitter, Threlte, Theatre.js, 21st.dev); flair.ai and getlayers.ai logged as further Production Engine candidates, not evaluated or adopted (OpenArt remains the confirmed vendor); several low-confidence names (GTA.Gallery, Posterlad, Peachweb.io, Motionsite, unsection.com, toolfolio.io) left explicitly unverified rather than guessed at.
- 2026-07-03 — **New downstream department created: Experience Engineering (20)** — Design's own interactive/experiential production discipline spun out as its own department rather than a Design sub-task, seeded from an owner-relayed external AI session. Design remains the asset-production layer; Experience Engineering assembles Design's outputs into whole interactive experiences. See `20_Experience_Engineering/EXPERIENCE_ENGINEERING_OS.md`. This **partially informs, does not resolve**, tracker item 48 (Design's own Motion/VFX/SFX/3D-5D Tool Registry): real candidate names now exist for the Transitions/VFX category (After Effects, Lottie) and the 3D/5D Motion category (Blender, Cinema 4D, Spline, Rive), sourced from Experience Engineering's founding session rather than a Design-direct owner decision — SFX/Sound Design remains genuinely unnamed even in that source material. Item 48 stays open; see `OWNER_INPUT_NEEDED.md` item 49.
- 2026-07-04 — **This department's Production Engine gets its first real customer: the Arika website project** (`20_Experience_Engineering/ARIKA_WEBSITE_PROJECT.md`, closes tracker item 51). Added an Example Imagery Brief to §10 — a ~5-6 scene shot list keyed to the real Brand Genome tokens and the existing Creative Digital Twin room doctrine, ready to execute via the existing Creative production pipeline (§4) the moment Canva and OpenArt are re-authorized. No image generated in this pass — blocked on connector re-authorization, not undone work.
- 2026-07-04 — **First real Production Engine output generated, same day.** Canva/OpenArt re-verified live (stale reading corrected). Ran a direct OpenArt `kling-3-omni` generation (not the full 4-agent pipeline, since this was a small time-boxed batch, not a campaign) for 3 of the brief's 5 scenes, spending all 40 available Free-plan credits. Caught one real "no visible AI artifacts" violation (garbled wall-sign text) and regenerated it clean rather than shipping it — the first real enforcement of this section's own quality standard. All 3 images downloaded into the website project and wired into real pages, not left unused. — Claude Code (Sonnet 5)
- 2026-07-04 — **Claude Design tried once OpenArt's credits ran out — real capability gap surfaced, not a quality problem.** Generated and actually exported (not just previewed) a Canva `generate-design` output for the remaining Executive Briefing Room scene: confirmed it produces decorative Canva template compositions, not full-bleed photography like OpenArt. Documents, for the first time with real evidence, that the two Production Engine vendors are not interchangeable — OpenArt for raw photographic scenes, Claude Design/Canva for graphic compositions (posters, social cards, decks). No image kept from this attempt; owner chose to hold off rather than force it. — Claude Code (Sonnet 5)
- 2026-07-07 — **KIE.ai added as a second Production Engine path for Nano Banana Pro (image) and Seedance (video)** — owner-supplied, already has a real API key. Unlike OpenArt/Canva, KIE.ai is a plain REST API, not an MCP server, so this is the department's first real code build rather than a documented-only registry row: `19_Design/design-plugin` (§13), a small TypeScript package with a shared `KieClient` and two model connectors, 13 tests passing against a mocked API. Directly addresses the OpenArt Free-plan credit exhaustion hit during the Arika website imagery pass (§10). Cross-referenced in `13_Tech_Stack/TECHSTACK_OS.md` §3. — Claude Code (Sonnet 5)
- 2026-07-07 — **KIE.ai verified real and connected via a live API call**, same verification discipline as Notion/OpenArt (not just assumed from an API key being present): `KieClient.getCredits()` (`GET /chat/credit`) is a free, read-only balance check — added specifically so the key could be confirmed without spending a generation credit — and returned a real 80-credit balance. §3 and `13_Tech_Stack/TECHSTACK_OS.md` §3 updated from "owner has a live API key" to "confirmed real and connected." — Claude Code (Sonnet 5)
- 2026-07-07 — **KIE.ai's Nano Banana Pro used for its first real generation, closing the Example Imagery Brief (§10).** With OpenArt still at 0 credits and Claude Design already ruled out for photographic scenes (§10), called the connector's underlying job API directly for the two scenes the brief had never gotten to (Insights hub, Solutions hero) — both clean on the first attempt, 18 credits each, 62 of 80 remaining. Downloaded and visually confirmed both before wiring them into the live site (`20_Experience_Engineering/arika-website`), same discipline as every prior image in this brief. All 5 scenes in the original brief are now real and live. — Claude Code (Sonnet 5)

## 9. Risk / Incident Log

*(placeholder — empty. Note: the "no visible AI artifacts — human-realistic production" standard in §10 exists specifically because the owner flagged current AI-mockup drafts as needing to be elevated before use — treat any future asset that reads as visibly AI-generated as a quality failure, not a style choice.)*

## 10. Standards & SOPs Index

**No visible AI artifacts — human-realistic production.** Directly from owner feedback on the moodboard drafts shared during this planning session: current AI-generated imagery needs to be elevated to a premium, human-realistic standard before use in real production — this is a hard quality bar for the Production Engine (§3) and the Asset Library (§3), not a stylistic preference.

**Creative Digital Twin — environment-consistency doctrine.** Every environment the agency produces (website hero, podcast set, dashboard visuals, event booth, case-study backdrop, product-demo backdrop) should read as one persistent virtual enterprise rather than a new visual concept per campaign. Named rooms: Executive Lobby (website hero), Executive Briefing Room (podcast), Revenue Operations Center (live dashboards), Growth Innovation Lab (event booths), Strategy War Room (case studies), Automation Command Center (product demos). Campaigns take place in different rooms of the same world rather than inventing a new aesthetic each time — this is the concrete constraint the Environment Library (§3) is built to enforce.

**Canva folder architecture — campaign-first, not platform-first.** Top level: `Brand System / Campaigns / Templates / Reusable Assets / Motion Assets / Podcast Assets / Events / Presentations`. Each entry under `Campaigns/` holds `Storyboards / Generated Assets / Video / Carousel / Presentation / Thumbnail / Ads / Final`. "Instagram" or "LinkedIn" is never a folder — platform is an output property of a campaign asset, not an organizing axis (matches Content's Campaign Architecture, `04_Content/CONTENT_OS.md` §10, so both departments share one unit of work). **Built 2026-07-01** — real Canva folders now exist: root `Arika Agency` (`https://www.canva.com/folder/FAHOJbTJfgg`) containing all 8 top-level folders (Brand System, Campaigns, Templates, Reusable Assets, Motion Assets, Podcast Assets, Events, Presentations). Campaign-level sub-folders (`Storyboards/Generated Assets/...`) not yet created — no real campaign exists yet to scaffold one for.

**Brand Guidelines — REBUILT 2026-07-03 with real, confirmed values.** The original 2026-07-01 draft (`https://www.canva.com/d/xMP6V78U8ND5twJ`) had Claude-proposed hex values the owner rejected outright and deleted manually in Canva (no delete-design tool exists in this MCP connector; confirmed gone via API — "not found"). **Real, official Brand Guidelines document now built**: `https://www.canva.com/d/sca6VWTlDJq13x_` (9 pages — cover, color palette, typography, logo, brand voice, graphic asset toolkit, art direction/composition rules, status), filed in `Brand System`, reflecting the full owner-confirmed Brand Genome plus the finalized logo. **Real values now confirmed via the "Brand Genome"** (owner-run Gemini session, reviewed and adopted 2026-07-03 — full detail in `12_Branding/BRANDING_OS.md` §2): Deep Revenue Navy `#0E1B29`, Operator Charcoal `#1C1C1C`, Alabaster Cream `#F7F5F0`, Champagne Gold `#D4AF37`, Blush Rose Pink `#F3C1C6` (UGC accent only); typography unchanged core pair (Space Grotesk/Satoshi/Neue Montreal display, Inter/Manrope body) plus an Editorial UGC layer (Editorial New/Ogg/Caslon/Canela hook font, Architects Daughter markup accent). A new, corrected Canva Brand Guidelines document — and the actual manual entry into Canva's native Brand Kit panel — is follow-up execution, not done in this pass; confirm no further source material is pending first (owner's paste was cut off mid-message).

**Logo finalized and CONFIRMED FINAL 2026-07-03.** Real, canonical logo generated via `generate-design` (design type: logo), 4 candidates produced, owner reviewed, selected, and edited one: **"Commanding Logo Featuring Integrated Arrow 'A'"** (`https://www.canva.com/design/DAHOWKNcWuw/...`), filed in `Brand System`. Owner explicitly signed off on it as-is ("the logo is okay") — treat as final, not draft. Wordmark typefaces confirmed: "ARIKA AGENCY" in **The Seasons**, "GROWTH PARTNERS" in **The Youngest** — both verified real/available in Canva, specific to the logotype (distinct from the Brand Genome's general content typography). Lockup spec (standard convention, accepted by extension since no changes were requested): reference unit "X" = height of the integrated arrow flourish; exclusion zone = 1X on all sides; minimum size = 24px/10mm standalone mark, 40px/15mm full lockup; construction grid = square grid in X-unit subdivisions. Full detail: `12_Branding/BRANDING_OS.md` §2. **The full brand kit is now complete** — only `competitors` remains open agency-wide (item 47).

**Logo exported as a real file and put into production use, 2026-07-07.** Design confirms brand assets; Experience Engineering (20) consumes them — this closes that loop for the first time. `export-design` (PNG, transparent-background flag, 1200px) was called on the same confirmed design (`DAHOWKNcWuw`); the export itself came back with the design's own navy canvas intact (Canva can't strip a background that's a real design element, only true transparency in the source), which turned out to be exactly right: that navy is the same `#0E1B29` as the website's scheme background, so no visible edge appears at any crop size. Used `sharp` (already a dependency of `20_Experience_Engineering/arika-website`) to auto-detect content bounds and cut two variants — an icon-only mark and the full icon+wordmark+tagline lockup — rather than re-generating anything, since the source design was already final. Wired into the website's Navbar, Footer, and favicon (`app/icon.png`). No changes made to the Canva design itself. — Claude Code (Sonnet 5)

**Storyboard-before-generation.** Every video-format asset gets a storyboard (scene-by-scene: hook, visual, camera, voice, music, duration, prompt) before any generation happens, stored durably in Canva. When generation tooling improves, regenerate from the storyboard rather than starting over — the storyboard is the durable asset, not the generated output.

**Download promptly, don't rely on vendor-hosted result URLs.** Confirmed real for KIE.ai (2026-07-07): generated media auto-deletes 14 days after creation (log/metadata records last 2 months) — `19_Design/design-plugin/docs/integrations.md`. Same discipline already practiced with OpenArt's output (downloaded straight into `arika-website/public/images/`, §10 below) now applies to every KIE.ai generation too: treat a returned result URL as temporary, download the real file before moving to the next pipeline stage.

**Example Imagery Brief — Arika website project (added 2026-07-04, `20_Experience_Engineering/ARIKA_WEBSITE_PROJECT.md`).** The first real customer of this department's Production Engine. **Executed 2026-07-04**: Canva and OpenArt were re-checked live (the "needs re-authorization" reading was stale) and 3 of the 5 scenes below were generated directly via OpenArt's `kling-3-omni` model — not the full Storyboard Generator → Production Engine Coordinator → Brand & Environment Consistency Checker → Canva Assembler pipeline, since this was a small, time-boxed batch rather than a full campaign requiring Canva filing. All 40 available Free-plan credits were spent (30 on the 3 kept images, 10 on one necessary retry — see below).

| Scene | Purpose | Environment (Creative Digital Twin room) | Status |
|---|---|---|---|
| Home hero (repurposed as "How We Work" image) | Replace the pasted placeholder hero mockup with the real brand direction | Executive Lobby | **Generated & wired in** (`hero-executive-lobby.png`) |
| Framework/Methodology page | "How we think" visual | Strategy War Room | **Generated & wired in** (`strategy-war-room.png`) |
| Solutions/outcome page | Outcome-led sales page visual | Revenue Operations Center | **Generated, repurposed for "Our Methodology" instead** (`revenue-operations-center.png`) — the Home/Frameworks pages used it there; the Solutions page itself got its own scene below once `PageHeader` gained an image slot |
| Insights hub | Resource library visual | Executive Briefing Room | **Generated 2026-07-07 via KIE.ai Nano Banana Pro** (`insights-intelligence-pod.png`) — added as a header image once the hand-built `PageHeader` component gained an optional `image` prop |
| Solutions page header | Outcome-mapping visual | Revenue Operations Center (variant: outcome-mapping room) | **Generated 2026-07-07 via KIE.ai Nano Banana Pro** (`solutions-outcome-room.png`), same `PageHeader` image-prop addition |
| Case Study layout | Template layout (no real client data yet — structure only) | Strategy War Room | **Covered by reusing the 3 OpenArt-generated images** across the Case Studies page's Diagnose/Build/Measure stages — deliberately not given new stand-in imagery, see below |

**One real quality defect caught and fixed**: the first Revenue Operations Center generation contained a garbled, nonsensical wall-sign text artifact — a classic AI-generation failure. Per this section's own "no visible AI artifacts" standard (below), this was treated as a quality failure, not shipped, and regenerated with explicit no-text instructions; the clean retry is what's actually in use.

**Claude Design tested as an OpenArt fallback, real finding: not a substitute for this use case.** Once OpenArt's 40 credits were exhausted, tried Canva's `generate-design` tool (the real MCP surface behind "Claude Design," §3) for the remaining Executive Briefing Room scene, using Arika's real Canva brand kit. Verified the actual output by converting a candidate to a real design and exporting a real PNG (not judging from a thumbnail): it produces composed Canva *template* graphics — decorative sparkles, dot-grids, a sunburst, a small photo inset in an arch frame — on-brand in color but not the clean, full-bleed photographic background OpenArt produced. This is a real capability difference between the two Production Engine vendors, not a quality miss: Claude Design/Canva is suited to graphic compositions (posters, social cards, decks), OpenArt to raw photographic scenes. Owner chose to hold off on further imagery rather than force the mismatch.

**Brief fully executed 2026-07-07 via KIE.ai's Nano Banana Pro connector — the department's first real generation call through `19_Design/design-plugin` (§13), not just the connector's build/test.** With OpenArt's credits still at 0 and Claude Design already ruled out for photographic scenes, `NanoBananaProConnector.generateImage()`'s underlying job API was called directly (16:9, 1K, `png`) for the two remaining scenes — both came back clean on the first attempt, no text-artifact retry needed, 18 credits each against the owner's real 80-credit KIE.ai balance. Confirmed via the same discipline as every other image in this brief: downloaded the real file and visually inspected it (not judged from a thumbnail or task-status alone) before wiring it into the site. Deliberately did not generate stand-in imagery for Case Studies (already covered by reuse) or Testimonials (an intentional honest empty state — fabricating client-adjacent photography there would misrepresent it as real client evidence, which this department's own honesty discipline rules out). All 5 scenes in this brief are now real, generated, and live on the site.

Every scene must key to the **real Brand Genome** (`12_Branding/BRANDING_OS.md` §2), not the generic aesthetic in the owner's pasted reference images:
- **Color**: Deep Revenue Navy `#0E1B29` (dominant base), Champagne Gold `#D4AF37` (CTA/metric accent only), Alabaster Cream `#F7F5F0` (text), Operator Blush `#F3C1C6` (sparing UGC accent, never primary)
- **Type**: Space Grotesk / Satoshi / Neue Montreal (headline), Inter / Manrope (body)
- **Imagery doctrine**: the Anti-Stock Law (no posed stock-photo composition — every image reads as an authentic real-time capture) and the Executive Infrastructure × Editorial UGC double-helix (glass/metal/architectural grids blended with raw/candid photography, oversized serif headline overlays, oval stickers)
- **No visible AI artifacts** — this file's existing hard quality bar (above) applies in full; these are meant to look like real production stills, not AI-mockup drafts

Filed under `Campaigns/` in the real Canva folder structure once generated — exact sub-folder naming is a Canva Assembler judgment call at execution time (a whole-website project doesn't map cleanly onto the existing campaign-first convention; decide then, don't pre-invent now).

## 11. RACI / Ownership

| Function | Responsible | Accountable | Consulted | Informed |
|---|---|---|---|---|
| Creative asset production | Design (19) | Design (19) | Branding (12), Content (04) | Marketing (03), Offer (02) |
| Canva folder/campaign structure | Design (19) | Mary Thuo | Content (04) | All departments |
| Design Language MCP/API (once built) | Design (19) | Mary Thuo | Tech Stack (13) | All departments |
| Motion primitive & asset handoff to Experience Engineering | Design (19) | Design (19) | Experience Engineering (20) | Marketing (03) |

## 12. Triggers / Automation Hooks

**Agent-to-agent handoffs are now real** (the agents exist and can be invoked directly), but nothing below is *automated* — there is no real trigger/automation platform wiring any of this to fire without a person or Claude Code invoking the next agent manually. Two different things, not to be conflated:

- **Real, invokable today**: Storyboard Generator → Production Engine Coordinator → Brand & Environment Consistency Checker → Canva Assembler (§4) — each handoff is a real cross-reference between real subagent files, callable in sequence right now via the Agent tool.
- **Still a placeholder, but now fully specified**: a Content brief reaching a real "ready for design" status in Notion automatically *triggering* the Storyboard Generator without anyone invoking it manually. This is designed in full — a durable, cron-based Anthropic cloud routine, with a human gate before any credit-spending step — as **Automation (16)'s "Creative Pipeline Automation (Design 19 trigger)"** (`16_Automation/AUTOMATION_OS.md` §3, §4, §12; real row in `00_Agency_Governance/AUTOMATION_APPROVAL_MATRIX.md`). Not yet live — blocked on 2 real prerequisites (`OWNER_INPUT_NEEDED.md` items 54-55): a Notion MCP connector for the cloud-routines system, and verification of the real Publishing Status field values.

## 13. Existing OS Sub-Layer

**First real code layer, added 2026-07-07: `19_Design/design-plugin/`.** A small TypeScript package (mirrors `09_Finance/finos-plugin`'s connector pattern, but standalone — not the same `IntegrationConnector`/`FinancialEvent` contract, which is Finance-specific) providing two real Production Engine connectors against KIE.ai's job API:

- `src/integrations/kie-client.ts` — shared `KieClient` (auth, `createTask`, `recordInfo` polling).
- `src/integrations/kie-nano-banana-pro.ts` — `NanoBananaProConnector.generateImage(...)`.
- `src/integrations/kie-seedance.ts` — `SeedanceConnector.generateVideo(...)`.

13 tests pass against a mocked KIE API (`npm test`, no real credits spent by the test suite). Needs `KIE_API_KEY` in `19_Design/design-plugin/.env` (owner has a real key; `.env.example` documents the shape, `.env` itself is gitignored). Full usage: `19_Design/design-plugin/docs/integrations.md`.

`DESIGN_LANGUAGE_SYSTEM.md` and `DESIGN_INSPIRATION.md` (this folder) remain architecture/reference documents, not executable systems.

## 14. Raw Archive Pointer

No department-local raw drafts — this is a genuinely new department with no inherited backlog, same status as Tech Stack (13) at its creation. Source material for this file is the owner planning session that created it (moodboard imagery and production-architecture vision) plus cross-references into `12_Branding/BRANDING_OS.md` (identity upstream) and `04_Content/CONTENT_OS.md` (briefs upstream).

## 15. Changelog

- 2026-07-01 — Built the real Canva folder structure (root `Arika Agency` + 8 top-level folders, §10) and a 5-page Brand Guidelines draft document filed in `Brand System`, with Claude-proposed (not owner-confirmed) hex values so Canva could render real swatches. Confirmed via Canva's `help` tool that the native Brand Kit panel (colors/fonts) is UI-only, not programmable through this MCP connector — flagged rather than worked around. — Claude Code (Sonnet 5)
- 2026-07-03 — **Production Engine vendor confirmed: OpenArt** (closes tracker item 45), plus Claude Design registered as an already-available visual-generation capability (Claude Pro plan, no new account needed). Cross-reference `13_Tech_Stack/TECHSTACK_OS.md` for the registry entries and `00_Agency_Governance/GO_LIVE_CHECKLIST.md` Phase 5 for setup status. — Claude Code (Sonnet 5)
- 2026-07-03 — **Retracted the Claude-proposed brand-kit draft** (§10) — owner rejected it outright; the Canva document still exists (no delete-design tool available in this MCP connector) but is dead/superseded, not reference material. Rework starts fresh. — Claude Code (Sonnet 5)
- 2026-07-03 — **Brand Genome adopted** as the real, confirmed color/typography values (Deep Revenue Navy #0E1B29, Operator Charcoal #1C1C1C, Alabaster Cream #F7F5F0, Champagne Gold #D4AF37, Blush Rose Pink #F3C1C6; Editorial UGC typography layer added) — sourced from an owner-run Gemini session, reviewed and confirmed. Cross-reference `12_Branding/BRANDING_OS.md` §2 for full detail and sourcing honesty note. New Canva Brand Guidelines document not yet rebuilt — pending confirmation that no further truncated source material is coming. — Claude Code (Sonnet 5)
- 2026-07-03 — **Logo finalized** — real logo generated in Canva, owner selected/edited one of 4 candidates, filed in `Brand System`. The Seasons + The Youngest confirmed as wordmark typefaces (verified real). Lockup spec proposed against the real file, pending owner sign-off. — Claude Code (Sonnet 5)
- 2026-07-03 — **Owner deleted the retracted Brand Guidelines draft manually in Canva** (confirmed gone via API — "not found"), and made a further adjustment to the logo (edit timestamp/version confirmed, exact change not visible on this end). — Claude Code (Sonnet 5)
- 2026-07-03 — **Owner confirmed the final logo explicitly** ("the logo is okay") — full brand kit (colors, typography, logo) now complete. Only `competitors` remains open agency-wide. — Claude Code (Sonnet 5)
- 2026-07-03 — **Rebuilt the Brand Guidelines document** (`https://www.canva.com/d/sca6VWTlDJq13x_`, 9 pages, filed in `Brand System`) with the real, confirmed Brand Genome values and finalized logo — replaces the deleted 2026-07-01 draft. — Claude Code (Sonnet 5)
- 2026-07-03 — **Added the Motion/VFX/SFX/3D-5D Tool & MCP Registry** (`DESIGN_LANGUAGE_SYSTEM.md` §2a, skeleton — Remotion named-not-connected, 3 categories open) **and the Inspiration Brackets system** (`DESIGN_INSPIRATION.md`, format-first + style-tag structure, currently empty) — both per explicit owner request during this session. Registered Remotion in `13_Tech_Stack/TECHSTACK_OS.md` §3 as owner-named/not-connected. Added `OWNER_INPUT_NEEDED.md` item 48 (real tool names/links needed) and 2 new items in `GO_LIVE_CHECKLIST.md` Phase 7. — Claude Code (Sonnet 5)
- 2026-07-03 — **New downstream department: Experience Engineering (20) created**, spinning Design's interactive/experiential-experience ambitions out into their own department rather than expanding Design's own mandate. Updated §1 Identity, §3 Production Engine row, §11 RACI, §8 Decision Log. Partially informed (not resolved) tracker item 48 with real candidate tool names from Experience Engineering's founding session — updated `DESIGN_LANGUAGE_SYSTEM.md` §2a accordingly. — Claude Code (Sonnet 5)
- 2026-07-04 — **Agent Roster built** — 6 real Claude Code subagents (§5), Design's first agent layer of any kind. Mapped directly to this file's own existing capabilities (Production Engine, Creative Digital Twin, Canva structure, Inspiration Brackets, Asset Library) — no external source material involved, unlike Experience Engineering (20)'s roster. — Claude Code (Sonnet 5)
- 2026-07-04 — **Workflow Index (§4) and Triggers (§12) updated** to name the real agent performing each step, now that the roster is live — added 2 new workflows (Asset reuse check, Inspiration intake) that only became expressible once real agents existed. Explicitly distinguished "agent-to-agent handoffs are real and invokable" from "nothing is automated yet" in §12, so this isn't mistaken for real trigger automation. — Claude Code (Sonnet 5)
- 2026-07-04 — **getlayers.ai corrected**: originally logged here as an unconfirmed Production Engine candidate; owner supplied the real URL and it turned out to be an AI-native prompt/template library (landing pages, 3D scenes, backgrounds), not a generation engine. Removed from this row, reclassified into `DESIGN_INSPIRATION.md`'s Reference Galleries list.
- 2026-07-04 — **Owner-supplied tool/inspiration list triaged and filed.** 18 raw tool/gallery names from the owner (flair.ai, GTA.Gallery, Khroma, Designspells, Posterlad, Theatre.js, Spline, Threlte, Peachweb.io, Three.js, Motionsite, 21st.dev, Framer Motion, unsection.com, screendesign, Jitter, toolfolio.io, getlayers.ai — several typo'd in the original) captured verbatim with an honest confidence rating in `DESIGN_INSPIRATION.md`, per no-silent-invention: only names Claude could confidently identify got a purpose note, the rest flagged unverified. Distilled the confident ones into `DESIGN_LANGUAGE_SYSTEM.md` §2a (4 new rows: Khroma, Jitter, Threlte+Theatre.js, 21st.dev) and `20_Experience_Engineering/EXPERIENCE_TECH_STACK.md` §2 (Threlte, Theatre.js), and logged flair.ai/getlayers.ai as unadopted Production Engine candidates (§3). Further informs but does not resolve `OWNER_INPUT_NEEDED.md` item 48. — Claude Code (Sonnet 5)
- 2026-07-04 — **Real automation specified** for this department's §12 trigger (Notion status → Storyboard Generator) — cross-referenced from the new "Creative Pipeline Automation (Design 19 trigger)" built in `16_Automation/AUTOMATION_OS.md`. Not yet live — 2 owner-actionable prerequisites tracked as `OWNER_INPUT_NEEDED.md` items 54-55. — Claude Code (Sonnet 5)
- 2026-07-07 — **Built `19_Design/design-plugin`, Design's first real code layer** (§13, previously "none yet"): a `KieClient` shared job-API client plus `NanoBananaProConnector` and `SeedanceConnector`, wrapping KIE.ai's `createTask`/`recordInfo` endpoints for the Nano Banana Pro image model and Seedance 1.5 Pro video model respectively. Owner supplied a real KIE.ai API key. 13 tests pass against a mocked KIE API (no real credits spent building/testing this). Registered in §3's Production Engine row and `13_Tech_Stack/TECHSTACK_OS.md` §3 as a second image/video path alongside OpenArt — not a replacement, since OpenArt still covers a much broader model catalog in one account. — Claude Code (Sonnet 5)
- 2026-07-07 — **Example Imagery Brief (§10) fully closed** — the 2 scenes still open since 2026-07-04 (Insights hub, Solutions hero) generated via KIE.ai Nano Banana Pro (direct job-API call, not the design-plugin package invoked at runtime), both clean on the first attempt, 18 credits each. Downloaded, visually verified, and wired into `20_Experience_Engineering/arika-website` (required adding an optional `image` prop to that project's hand-built `PageHeader` component). All 5 scenes in the original brief are now real, generated, and live — see `20_Experience_Engineering/ARIKA_WEBSITE_PROJECT.md` §6, §9 for the website-side record. — Claude Code (Sonnet 5)
- 2026-07-01 — Department created. Seeded from an owner planning session: owner-supplied moodboard direction and detailed creative-infrastructure vision (asset libraries, production engine, Canva-as-assembly, campaign-first folder structure, Creative Digital Twin environment doctrine, Design Language MCP/API + skill library). Numbered 19 (not 18, which stays the inactive Cross-Domain Synthesis archive). Populated Capability Registry, Workflow Index, Standards & SOPs Index. Canva confirmed as the one genuinely live capability. Added to `GLOBAL_OS.md` §4 Department Index and `00_Agency_Governance/OWNER_INPUT_NEEDED.md`/`GO_LIVE_CHECKLIST.md`. — Claude Code (Sonnet 5)

## 16. Memory / Feedback Loop / Cadence

*(placeholder — structure only, no agent roster exists yet to generate real memory/feedback entries; see §5.)* Once this department has a real or code-based agent roster (per the Tier 1 pattern in `05_Sales/SALES_OS.md` §16), this section should define: **Memory** (where Decision/Learning/Prompt-Evolution logs live), **Feedback Loop** (what happens when a §7 KPI misses threshold), and **Cadence** (which of the 7 Cognitive Calendars — `00_Agency_Governance/AGENCY_REVENUE_TARGETS.md` §4 — this department's workflows run against, and how often).
