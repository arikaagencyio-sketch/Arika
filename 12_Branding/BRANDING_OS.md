# Branding — Department OS

**Department:** Branding (12)
**Position in flow:** Horizontal support layer — feeds Marketing, Sales, Client Success, and Agency Governance with brand architecture, narrative, and identity systems.
**Mandate:** Own brand definition, positioning, narrative engineering, and visual/identity systems for the agency and (as a service) for clients.
**Owner:** Mary Thuo

> See [`GLOBAL_OS.md`](../GLOBAL_OS.md) for how this file fits into the whole system. Read that first if you haven't.

---

## 1. Identity

Branding defines and governs how the agency (and client brands it manages) present themselves — architecture, narrative, positioning, visual cognition, cultural intelligence. Its code layer (`bois/`, "Brand Operating Intelligence System") frames itself as "a stateful, retrieval-driven, agent-orchestrated execution ecosystem" with a non-negotiable runtime rule: if retrieval evidence is missing, the system must stop in a "retrieval failure state" rather than generate generic branding output — i.e., it's architected specifically against hallucinated brand work, the same principle this whole repo's migration follows. Recurring doctrine across the raw drafts: **"Branding = Why you are chosen, Marketing = How you get seen"** — the two departments' boundary, stated by the source material itself (unlike several other department boundaries in this repo, which had to be imposed).

**Parent doctrine (2026-07-23).** The agency's commercial worldview, Operating Voice, and behavioral doctrine now live one level up, in Governance (`00_Agency_Governance/AGENCY_COMMERCIAL_DOCTRINE.md` — owner decision). Branding is the **brand-identity expression** of that parent: the Brand Genome, visual system, and brand narrative that make the doctrine *look and feel* like Arika. Governance owns *how the agency thinks and speaks commercially*; Branding owns *how it is recognized*. The "why chosen / how seen" boundary is unchanged; the Commercial Doctrine simply sits above both — and above the new Presence department (21), which must obey the Brand Genome when it expresses presence.

## 2. Status

**Content migration complete (2026-06-30) — correcting an earlier under-assessment, same pattern as Finance.** `bois/` is meaningfully more real than the original skeleton pass characterized it: **all 21 `core/` Python files contain real working logic — none are stubs**, a stronger ratio than Finance's finos-plugin (which had one confirmed stub). `bois/core/ingestion/pipeline.py` was actually **run** against this department's 40 raw drafts (`bootstrap_bois.py`), producing a real, auto-generated, exhaustive source-citation system (`bois/knowledge/_indexes/`: 40 documents, 14,109 tagged paragraphs, zero ingestion errors) — a stronger citation system than Finance's hand-curated `source-intelligence.md`, since it's reproducible by rerunning the script rather than manually maintained. A full smoke test was run end-to-end against a hypothetical client ("Sample Nairobi Laundry," explicitly labeled as a sample).

**Tracker item 30 resolved 2026-06-30 — BOIS now has a real, non-hypothetical client.** `bois/executions/run_arika_agency_brand.py` builds a real `ClientObject` for Arika Agency itself from facts already confirmed elsewhere in this repo (`AGENCY_VISION.md`, `AGENCY_REVENUE_TARGETS.md`, `02_Offer/OFFER_OS.md`, `01_Sector/SECTOR_OS.md`, the confirmed "Revenue Infrastructure Partner" positioning, and the Kenya/global geography confirmation) — **confirmed run**, producing a real client workspace (`bois/clients/arika-agency/`) and a complete retrieval bundle (zero missing sources across all 6 required sources, 11 of 20 agents activated) for the task "define Arika Agency's own core brand identity, positioning, and voice." Real, confirmed fields: sector/sub-sector, audience (3-tier ICP), services (the 11-offer catalog), pricing-model summary, market position, communication style and emotional positioning (both sourced verbatim from `AGENCY_VISION.md`/`AGENCY_REVENUE_TARGETS.md`'s ROCBO doctrine), governance rules, stakeholder notes. **Explicitly left empty, not invented** — genuinely unconfirmed: `competitors`. `visual_preferences`, `typography_preferences`, `color_preferences` — see the 2026-07-01 entry immediately below for a partial resolution.

**Visual/typography/color direction partially resolved (2026-07-01), then superseded (2026-07-03) — see below.**

**Brand Genome adopted (2026-07-03) — closes the color/typography portion of this gap for real.** The owner ran an extensive separate session with Gemini (Google's AI) to develop Arika's visual identity in depth, culminating in a "Brand Genome" document — explicitly a Gemini-generated creative proposal, not a human designer's final sign-off, same category of source as Claude's earlier (rejected) draft, just a more extensive, deliberate session. The owner reviewed it and confirmed the **Brand Genome version** (the later, evolved palette — after a deliberate pivot to blend the original executive/enterprise system with an "Editorial UGC" layer) as canonical, explicitly choosing it over an earlier "Design System Manual" palette from the same session. Confirmed real values:

- **Color palette** (`color_preferences`): Deep Revenue Navy `#0E1B29` (dominant, 60% — base canvas, dark panels), Operator Charcoal `#1C1C1C` (structural, 20% — grids, textured backgrounds), Alabaster Cream `#F7F5F0` (high-contrast text — headlines, body on dark), Champagne Gold `#D4AF37` (metric/CTA accent — checkmarks, highlights, ROI data), Blush Rose Pink `#F3C1C6` (UGC sticker accent — oval badges, conversational highlights only, not a primary brand color)
- **Typography** (`typography_preferences`): Display/Headline — Space Grotesk, Satoshi, Neue Montreal (original executive layer) plus Editorial New/Ogg/Caslon/Canela (high-fashion serif "hook" font for the Editorial UGC layer); Body — Inter, Manrope; Markup/annotation accent — Architects Daughter (handwritten-style script, used sparingly for hand-drawn arrows/annotations)
- **Visual direction** (`visual_preferences`): a deliberate two-layer system — "Executive Infrastructure" (premium enterprise: glass/metal surfaces, cinematic lighting, architectural grids) blended with "Editorial UGC" (raw/candid photography, oversized serif headline overlays, oval stickers, notepad-style checklists, hand-drawn arrow annotations) — described in the source material as "The Arika Double-Helix"

**Full Brand Genome detail received 2026-07-03 (source paste completed, no longer truncated).** Exact usage architecture, confirmed real:
- Color weighting: Deep Revenue Navy 60% (dominant/base canvas), Operator Charcoal 20% (structural/grids), Alabaster Cream (high-contrast text/headlines), Operator Blush `#F3C1C6` 10% ("the Hook" — conversational/sticker use only, same color as "Blush Rose Pink" named earlier in the same source), Pipeline Gold `#D4AF37` 10% ("the Metric" — same color as "Champagne Gold" named earlier; charts/data/checkmarks)
- Typography styling rules: Editorial New/Ogg display hook — tracking -4% to -6% (letters nearly interlock), leading 90% of font size (tightly stacked); Satoshi/Inter body — 0% tracking, +5% tracked for small all-caps utility labels; Architects Daughter markup accent — tilted -8° to +12°, used only to annotate a data point or chart peak
- Graphic asset toolkit (4 modular components): Cultural Oval Badge (capsule shape, Operator Blush or black fill, bold all-caps center text — e.g. "doing growth operator sh*t," "infrastructure build mode"); Torn Notepad Component (white/Alabaster Cream rectangle, `rgba(0,0,0,0.15)` drop-shadow, spiral-hole left margin — for checklists/frameworks); Native Platform Interventions (real IG/iOS UI elements superimposed on photography, e.g. "Ask me a question" stickers); Vector Sparks & Arrows (Y2K 4-point star glints near stats, messy single-stroke marker arrows linking text to photo subjects)
- Art direction ("Anti-Stock Law"): no posed/smiling stock-photo composition — every image must read as an authentic, real-time smartphone capture. Macro subjects: hands on a space-gray MacBook, iPad flowchart sketches, whiteboards with real API-routing diagrams. Environments: mid-century boardrooms, urban cafés, developer setups, city views
- Composition/grid rules: Rule of Thirds Layering (oversized text at the top third or down the left margin), Depth Composition (text layered *behind* a physical subject, e.g. behind an operator's shoulder), Contrast Alternation (never stack two heavy-text posts back to back — alternate hook / macro-tech-shot / interactive-UI layouts)

**Logo finalized 2026-07-03.** No prior logo file existed (confirmed) — a real, canonical logo was generated in Canva ("Commanding Logo Featuring Integrated Arrow 'A'", `https://www.canva.com/d/Q9Qmq2wvwj6utVB`, filed in `19_Design`'s `Brand System` folder), owner-selected from 4 candidates and lightly edited. **Logo wordmark typefaces confirmed**: "ARIKA AGENCY" set in **The Seasons** (elegant serif, verified real/available in Canva), "GROWTH PARTNERS" set in **The Youngest** (serif display, verified real/available in Canva) — these are the logo's own typefaces specifically, distinct from the Brand Genome's general content typography (Space Grotesk/Satoshi/Editorial New system used for marketing headlines/body, §2 above) — standard practice for a logotype to use its own dedicated font, separate from the brand's running-text system. **Lockup specs proposed** (standard convention, not Arika-specific invention, pending owner sign-off): reference unit "X" = height of the integrated arrow flourish; exclusion zone = 1X on all sides; minimum size = 24px/10mm (standalone mark) or 40px/15mm (full lockup); construction grid = square grid in X-unit subdivisions; standard incorrect-usage exclusions (no stretch/recolor/low-contrast backgrounds).

**Logo explicitly confirmed final (2026-07-03).** Owner reviewed the edited logo in Canva and confirmed it as-is ("the logo is okay") — the proposed lockup spec (exclusion zone, minimum size, construction grid) is accepted by extension, no changes requested. **The full brand kit — colors, typography, logo — is now completely resolved.** The only field still genuinely open in Arika's own brand identity is `competitors` (tracked separately as `00_Agency_Governance/OWNER_INPUT_NEEDED.md` item 47). Production execution against this direction (asset generation, Canva Brand Kit setup) is owned by the Design department (19) — see `19_Design/DESIGN_OS.md`.

**✅ Follow-up closed 2026-07-15 — the Brand Genome is now in the code, not just this file.** The step named above (entering these values into `bois/clients/arika-agency/`'s `client.json` and re-running `run_arika_agency_brand.py`) was still outstanding: the live `client.json` was written **Jun 30** and still carried `visual_preferences`, `typography_preferences`, and `color_preferences` as **empty arrays**, three days after this section recorded them as confirmed real. It was found and fixed while wiring synthesis, because it was directly in the path — BOIS' agents would otherwise have reasoned about Arika's own brand with no colors, typography, or visual direction. `run_arika_agency_brand.py` (the source of truth; `client.json` is its output) now carries all three, re-run confirmed: retrieval gate complete, 11 agents activated. **`competitors` remains genuinely empty** — item 47. See §8.

Like every department, **no real client names (other than the agency's own), brand names, pricing, or measured KPI values exist anywhere** — all scoring thresholds are designed defaults, not calibrated against real outcomes.

## 3. Capability Registry

`bois/core/`'s 21 modules + `bois/agents/AGENT_SYSTEM.md`'s 20-agent roster (§5) are the system's real capability set. Raw-draft capability concepts layered beneath them (status: draft/aspirational unless noted):

| Capability | Description | Status |
|---|---|---|
| Knowledge ingestion & tagging | Parses real `.docx` source files, SHA-256 checksums, 13 keyword-based tag rules, section-hint heuristics, repetition tracking | **Real, run** — `core/ingestion/{docx_reader,pipeline}.py` |
| Knowledge graph construction | Builds doc→paragraph→tag→entity→reinforcement graph via regex entity extraction (stage/system/agent/framework patterns) + tag co-occurrence matrix | **Real, run** — `core/knowledge/graph_builder.py` |
| Hybrid retrieval with hard gate | TF-IDF-style scoring (log-tf × idf) + tag-overlap + reinforcement boosting across 6 required sources; raises a runtime error if retrieval is incomplete rather than proceeding | **Real** — `core/retrieval/gated_retriever.py` |
| Agent orchestration & trigger routing | Assembles dynamic brand context, routes among 20 agents via sector/deliverable/geography/audience trigger rules (concrete examples: fintech, healthcare, luxury, laundry, b2b), builds per-agent task objects with constraints | **Real** — `core/orchestration/{registry,engine}.py` |
| Brand scoring / grading | 15 weighted scoring dimensions, each with its own threshold (70-78) and metric list; weighted-total computation, failed-dimension flagging, recommendation library | **Real** — `core/grading/engine.py` |
| Brand governance validation | Validates retrieval completeness, agent activation, 8 mandatory output-reasoning fields, 5 continuity-keyword checks (narrative/visual/culture/sales-marketing/operations) | **Real** — `core/governance/policy.py` |
| Presentation intelligence | 12 required presentation sections, each with a concrete "must explain" requirement; generates narrative-arc and quality-standard lists | **Real** — `core/presentation/engine.py` |
| Client workspace management | Per-client folder tree (memory/campaigns/presentations/visuals/governance/deliverables/timelines), slugified, with client.json | **Real** — `core/client_workspace.py` |
| Append-only memory | JSONL event log across 8 memory streams (client/campaign/visual/narrative/governance/market/trend/stakeholder) | **Real** — `core/memory/store.py` |
| Brand architecture typing | Branded House / House of Brands / Endorsed Brand / Hybrid Architecture (standard industry taxonomy) + a 5-layer "offer pyramid" + 6-stage client lifecycle | Draft/aspirational (Draft 2) |
| Positioning formula | "For [audience], [Brand] is the [category] that delivers [value] through [mechanism], unlike [alternative]" — repeated near-verbatim across multiple drafts | Draft/aspirational (Draft 32, echoed elsewhere) |
| Narrative engineering | 5-layer stack: Truth Core → Narrative Framework → Narrative Modules → Translation Layer → Feedback/Evolution | Draft/aspirational (Draft 23) |
| Psychographic intelligence ("DPIS") | 8-layer system: Identity → Emotional → Motivation → Cultural Signal → Behavioral → Narrative Resonance → Perception Feedback → Adaptive Decision Engine | Draft/aspirational (Draft 27) |
| Cultural alignment scoring | 5-dimension weighted Cultural Alignment Score (Awareness, Resonance, Behavioral Alignment, Identity Integrity, Evolution Capacity) — clearest conceptual ancestor of `BrandScoringEngine`'s 15 dimensions | Draft/aspirational (Draft 24), partially realized in code |
| Sector intelligence | 7-layer stack: Value Logic, Trust Mechanics, Risk Profile, Decision Dynamics, Category Codes, Narrative Space, Constraints | Draft/aspirational (Draft 19) |
| Legal branding / IP defense | 5 pillars (Identity Ownership, IP System, Usage/Control, Entity/Liability Structure, Enforcement/Defense) + an "enforcement pyramid" (Education→Warning→Restriction→Legal Action→Litigation) | Draft/aspirational (Draft 29) — note: generic business-entity content, not the agency's own legal infrastructure (that's Legal, 10) |
| Brand distribution & cadence | Micro/meso/macro cadence layers, "70-20-10 rule" for content mix | Draft/aspirational (Draft 25) |

**Note on "Narrative Engineering" (2026-07-03; updated 2026-07-15):** this stack is the **brand-identity narrative layer** — why the brand exists — distinct from Content (04)'s Story Architecture (content-sequencing layer, `04_Content/CONTENT_OS.md` §10) and Experience Engineering (20)'s Narrative Arc (experience-orchestration layer, `20_Experience_Engineering/EXPERIENCE_ENGINEERING_OS.md` §3). All three are real, non-competing concepts at different altitudes. **Now a 3-way distinction, not 4-way**: EE's two candidate arc drafts were reconciled on 2026-07-15 into one canonical arc — `Attention → Problem → Transformation → Proof → Offer` — so its internal Draft-A/Draft-B split no longer exists.

## 4. Workflow Index

| Workflow | Trigger | Steps | Output | Owner | Source |
|---|---|---|---|---|---|
| Knowledge bootstrap | Manual run | `bootstrap_bois.py`: scan raw drafts → checksum/ID → parse paragraphs → tag → build knowledge graph → write indexes | Populated `bois/knowledge/_indexes/` (documents.json, paragraphs.jsonl, knowledge_graph.json) | Mary Thuo | `core/executions/bootstrap_bois.py` — **confirmed run**, 40 docs / 14,109 paragraphs / 0 errors |
| Brand context assembly & agent routing | New client/brief | Assemble `DynamicBrandContext` → match sector/deliverable/geography/audience against `TRIGGER_RULES` → select agents from `BASE_AGENT_SEQUENCE` (7-agent base chain) + triggered specialists → build per-agent tasks | Routed agent task set | Mary Thuo | `core/orchestration/engine.py` |
| Gated retrieval | Agent task needs evidence | Query 6 required sources via hybrid (TF-IDF + tag + reinforcement) scoring → assemble bundle → **hard-fail (`RuntimeError`) if incomplete** rather than proceed with partial evidence | Retrieval bundle or explicit failure state | Mary Thuo | `core/retrieval/gated_retriever.py` |
| Brand scoring / audit | Deliverable or evidence set ready | Score across 15 weighted dimensions (manual `.score()` or evidence-based `.audit_from_evidence()` keyword-hit heuristic) → flag dimensions below threshold (70-78) → attach recommendations | Scored brand audit | Mary Thuo | `core/grading/engine.py` |
| Governance validation | Agent output produced | Check retrieval completeness, agent activation, 8 mandatory reasoning fields, 5 continuity-keyword checks | Pass/fail governance finding | Mary Thuo | `core/governance/policy.py` |
| Smoke test | Validation run | End-to-end run of bootstrap + orchestration + scoring against a hypothetical client | Confirmed working pipeline | Mary Thuo | `core/executions/smoke_test.py` — **confirmed run** against "Sample Nairobi Laundry" (explicitly labeled sample data) |
| Real brand definition (agency's own) | Manual run | Build real `ClientObject` for Arika Agency from confirmed-real repo facts → `create_client_workspace` → gated retrieval → agent routing → persist context | Real client workspace `bois/clients/arika-agency/`, complete retrieval bundle, 11 activated agents | Mary Thuo | `bois/executions/run_arika_agency_brand.py` — **confirmed run**, resolves tracker item 30 |

## 5. Agent Roster

**20 agents**, each with a full purpose/memory-access/validation contract in `bois/agents/AGENT_SYSTEM.md`, mirrored in code at `bois/core/orchestration/registry.py`'s `AGENT_REGISTRY` (docs and code in sync, same pattern as Finance's agent-prompts.md/financialAgentSpecs): **Master Orchestrator, Sector Intelligence, Audience Cognition, Cultural Intelligence, Brand Identity, Narrative Engineering, Visual Cognition, Brand Governance, Brand Evolution, Sales Alignment, Marketing Synchronization, Distribution Intelligence, Brand Production, Psychographic Intelligence, Symbolic Systems, Semantic Intelligence, Trend Intelligence, Brand Calendar, Trust and Authority, Presentation Intelligence**. A `BASE_AGENT_SEQUENCE` (7-agent base chain) always runs; the remaining agents activate via concrete `TRIGGER_RULES` keyed by sector (fintech/healthcare/luxury/laundry/b2b), deliverable type, geography, and audience.

**✅ WIRED 2026-07-15 — BOIS can now think.** The caveat that stood here since 2026-06-30 ("real spec/configuration/routing data, not executable AI… no code in this repo calls an LLM to run these agents") **is no longer true.** The owner's 2026-06-30 decision — **Claude (Anthropic API) backs all 20 agents** — is now implemented:

- **`bois/core/synthesis/engine.py`** — new. `BrandAgentRuntime` implements the **`output_synthesis`** stage, which `EXECUTION_STAGES` had always listed as stage 4 of 7 but which **no code implemented**. `agent_prompt()` built a prompt nothing consumed; `AgentResult` was a dataclass nothing produced. BOIS could route, retrieve, score, validate, and remember — it could not reason. Mirrors finos-plugin's `ClaudeAgentRuntime` deliberately (lazy client, structured JSON output, registry-supplied system prompt), so both grandfathered code layers share one reasoning shape.
- **`bois/requirements.txt`** — new; `anthropic>=0.69.0`. **Only synthesis needs it.** Ingestion, retrieval, knowledge-graph building, orchestration, scoring, governance, and memory remain stdlib-only and run with no key and no install.
- **`BrandCognitiveOrchestrator.synthesize()`** — new; closes `agent_activation → output_synthesis → governance_validation` and persists every agent's reasoning to the `governance` stream plus its `memory_updates` to the `client` stream.
- **`bois/executions/run_brand_task.py`** — new; JSON-in/JSON-out entry point. `synthesize: false` is a **first-class mode**, not a degraded one — routing/retrieval/governance were always real and still run without Claude.

**Runtime exposure — 3 agents, not 20** (`.claude/agents/branding-*.md`, `execution: bois`):

| Agent | `bois_mode` | Class | Does |
|---|---|---|---|
| `branding-brand-definition` | `define` | 2 | Runs the full orchestra: gated retrieval → 20-agent routing → synthesis → governance |
| `branding-brand-audit` | `audit` | 2 | The 15-dimension `BrandScoringEngine` |
| `branding-governance-validator` | `govern` | 2 | Retrieval completeness, agent activation, 8 mandatory fields, 5 continuity checks |

**Why 3 and not 20** (owner-confirmed): BOIS' 20 agents are a **routed chain inside one brand task** (`BASE_AGENT_SEQUENCE` + `TRIGGER_RULES`), unlike Finance's 7 independent event listeners. Exposing them individually would bypass `route_agents()` — the thing that makes BOIS smart. `arika-runtime` invokes the orchestra; BOIS conducts it. The 20 stay internal and keep their routing.

**Verified end-to-end (2026-07-15), no API key required:** `arika run branding-brand-definition --input '{"client_id":"arika-agency","synthesize":false}'` → TypeScript → subprocess → Python BOIS → **11 agents routed, retrieval gate complete (zero missing sources)** → JSON envelope → memory line written to `12_Branding/_memory/runtime.jsonl`. With `synthesize: true` and no key, it **degrades gracefully** with a clear install message while still reporting that routing and retrieval ran — it does not crash. `arika list` → 64 agents; `npm test` → 9/9.

## 6. Skill Library Index

*(placeholder — no separate skill library exists; agent capabilities are defined directly in the Agent Roster's contracts above, same as Finance)*

## 7. KPI Dictionary (department-local)

**All thresholds below are generic designed defaults, not real measured figures** — same caveat as every other department, though here (like Finance) the numbers double as actual code logic, not just narrative targets.

| Metric | Formula | Threshold found | Source | Owner | Cadence |
|---|---|---|---|---|---|
| Brand Score (composite) | Weighted sum across 15 scoring dimensions | Per-dimension thresholds 70-78; weights sum to ~1.0 | `core/grading/engine.py` (`BrandScoringEngine`) | Mary Thuo | Per-deliverable/audit |
| Cultural Alignment Score | 5 sub-metrics: Resonance Rate, Emotional Sentiment Score, "Seen" Index, Promise-to-Behavior Consistency, Core Identity Stability, Adaptation Speed | Named formula, no real values | Draft 24 — conceptual ancestor of the code's scoring engine | Mary Thuo | *(unset)* |
| Brand Diagnostic Score | 0-5 per layer × 8 layers, total /40, bands "Fragmented" → "Category Dominant" | Named, illustrative | Draft 3 | Mary Thuo | *(unset)* |
| Sector Brand Intelligence Index | 7 dimensions, 1-10 scoring | Named, illustrative | Draft 19 | Mary Thuo | *(unset)* |
| Governance KPIs | Brand Trust Score, Infringement Incidents, Enforcement Response Time | Named only, no values | Draft 29 | Mary Thuo | *(unset)* |
| Retrieval completeness | Bundle assembled across 6 required sources | Hard gate — incomplete retrieval raises a runtime error rather than degrading silently | `core/retrieval/gated_retriever.py` (`RetrievalGate`) | Mary Thuo | Per-query |

Cross-reference `00_Agency_Governance/AGENCY_KPI_DICTIONARY.md` for the agency-wide dictionary; none of these brand-scoring metrics currently have an agency-wide analog.

**Owner decision (2026-06-30, tracker item 29): keep the `BrandScoringEngine`'s generic 70-78 dimension thresholds as-is** (confirmed via direct code inspection — `core/grading/engine.py`'s `ScoreDimension` definitions). Same reasoning as Finance's KPI thresholds (item 26): no real BOIS run against a real client exists yet to calibrate against.

## 8. Decision Log

- **2026-07-15 — 🔴 CURED A LATENT CONTRACT BUG that would have failed governance on every synthesized run, forever.** Found while wiring synthesis. **Three separate lists defined what a BOIS agent must return, and they had silently diverged:**

  | Field | `agent_prompt()` (registry.py) | `build_agent_tasks()` (engine.py) | `MANDATORY_OUTPUT_FIELDS` (policy.py) |
  |---|:--:|:--:|:--:|
  | strategic / psychological / symbolic / cultural_reasoning, operational_implications | ✅ | ✅ | ✅ |
  | `validation_risks`, `memory_updates` | ✅ | ✅ | ❌ |
  | **`emotional_reasoning`, `visual_reasoning`, `positioning_rationale`** | ❌ | ❌ | ✅ |

  Governance **required** three fields that nothing ever **asked the model for**. Any wiring done against `expected_outputs` would have produced output that failed 3 of 8 mandatory `high`-severity checks on every single run — and the failure would have looked like the agents' fault. **The bug was invisible precisely because synthesis had never been wired**: the gate was validating output that never arrived.
  **Cure (owner-confirmed among 3 options): the 10-field superset.** `AGENT_OUTPUT_CONTRACT = MANDATORY_OUTPUT_FIELDS + SUPPLEMENTARY_OUTPUT_FIELDS` is now the single canonical list in `policy.py`; `agent_prompt()` and `build_agent_tasks()` both **import** it rather than re-listing fields, so it cannot drift again. Purely additive — no existing behavior changed. The 8 governance-mandatory fields now pass by construction; `validation_risks` feeds `AgentResult.validation_notes` and `memory_updates` feeds the memory store — both real downstream consumers that would otherwise have received nothing. Verified: a contract-compliant output now returns **zero governance failures**. — Claude Code (Opus 4.8)
- **2026-07-15 — BOIS' missing `output_synthesis` stage implemented; the 20 agents can now reason** (§5). Implements the owner's 2026-06-30 decision (Claude/Anthropic API backs all 20 agents), which had stood unbuilt. **Wiring location (owner-confirmed among 3 options): inside BOIS (Python), mirroring finos-plugin** — not in `arika-runtime` (TypeScript). Reasoning: BOIS' governance, 15-dimension scoring, and append-only memory are all Python and the output must return to them; `EXECUTION_STAGES` places `output_synthesis` *between* `agent_activation` and `governance_validation`, i.e. **inside** BOIS' own pipeline; and `finos-plugin` set the precedent that a grandfathered code layer owns its reasoning while `arika-runtime` wraps it. `arika-runtime` therefore **delegates via subprocess** (`src/wrappers/bois.ts`) and never reimplements the call. **BOIS is left structurally intact** — the only additions were its missing stage and a JSON entry point. — Claude Code (Opus 4.8)
- **2026-07-15 — Closed the stale `client.json`.** Arika's real BOIS client object (`bois/clients/arika-agency/memory/client.json`, written **Jun 30**) still had `visual_preferences`, `typography_preferences`, and `color_preferences` as **empty arrays** — three days *after* the Brand Genome was adopted (2026-07-03) and recorded as owner-confirmed real in §2. §2 itself named entering these values and re-running `run_arika_agency_brand.py` as outstanding follow-up. **This was directly in the path of wiring**: synthesis would have had Arika's own brand agents reason about Arika's brand with no colors, no typography, and no visual direction. Populated `run_arika_agency_brand.py` from §2's confirmed values (the script is the source of truth; `client.json` is its output) and re-ran it — retrieval gate complete, 11 agents activated. **`competitors` deliberately left empty** — genuinely unconfirmed, still `OWNER_INPUT_NEEDED.md` item 47. — Claude Code (Opus 4.8)
- 2026-07-01 — Owner-supplied moodboard imagery accepted as the agency's real visual/typography direction (palette names Deep Navy/Charcoal Gray/Silver Mist/Accent Gold; typeface direction Space Grotesk/Satoshi/Neue Montreal display, Inter/Manrope body) — see §2. Exact hex/weight specs and `competitors` remain open; asset production execution against this direction is now owned by the new Design department (19), not Branding.
- 2026-07-03 — **Brand Genome adopted as the real, confirmed color/typography palette**, superseding the 2026-07-01 direction-only entry. Sourced from an extensive owner-run Gemini session (Gemini-generated proposal, owner-reviewed and confirmed — same category of source as Claude's earlier rejected draft, more extensive/deliberate). Owner explicitly chose the later "Brand Genome" palette over an earlier "Design System Manual" palette from the same session. See §2 for full values. Logo lockup exact pixel specs and `competitors` remain open.
- 2026-07-03 — **Full Brand Genome detail captured** (the earlier paste had been cut off mid-message) — exact color-weighting percentages, typography tracking/leading rules, the 4-part graphic asset toolkit, art-direction rules, and composition/grid rules all now documented in §2. Surfaced that no single canonical logo file (vector or otherwise) is confirmed to exist yet — a precondition for defining real logo lockup specs, addressed in `19_Design/DESIGN_OS.md` §10.
- 2026-07-03 — **Reconciled a 4-way "narrative" naming collision** created by the founding of Experience Engineering (20): this department's own Narrative Engineering stack (§3, brand-identity layer) is distinct from Content (04)'s Story Architecture (content-sequencing layer) and Experience Engineering's two candidate Narrative Arc drafts (experience-orchestration layer) — cross-reference note added to §3, mirrored in `04_Content/CONTENT_OS.md` §10 and `20_Experience_Engineering/EXPERIENCE_ENGINEERING_OS.md` §1.

## 9. Risk / Incident Log

*(placeholder — empty. Note: the retrieval gate's hard-fail behavior — refusing to generate output on incomplete evidence — is itself a real, implemented risk control, not a gap. No real incidents logged yet; the system has now run against one real client — Arika Agency itself, `bois/clients/arika-agency/` — in addition to the hypothetical "Sample Nairobi Laundry" smoke test.)*

## 10. Standards & SOPs Index

**Architecture doctrine** (`bois/documentation/COGNITIVE_INFRASTRUCTURE_ARCHITECTURE.md`): BOIS is "a stateful, retrieval-driven, agent-orchestrated execution ecosystem" across 6 layers (Knowledge, Memory, Retrieval, Agent Orchestration, Governance/Scoring, Evolution). Non-negotiable rule: missing retrieval evidence → stop in "retrieval failure state," never generate generic output.

**Recurring principles from raw drafts** (treat as working doctrine, several echoed independently across multiple files — strong internal signal):
- "Branding = Why you are chosen, Marketing = How you get seen" (Draft 1) — this department's own stated boundary with Marketing.
- "Visual design is downstream of strategy, not upstream" (Draft 5, echoed in Draft 6).
- "Consistency without adaptation becomes irrelevance; adaptation without governance becomes chaos" (Draft 21).
- "A brand is only as strong as its ability to defend itself" (Draft 29).
- Positioning requires trade-offs — "appeal to everyone, lose clarity" (Draft 32).

**11 additional spec docs**, one per `bois/` subfolder, mutually consistent with the architecture doc and the code (not boilerplate): `bois/agents/AGENT_SYSTEM.md`, `bois/governance/GOVERNANCE_SYSTEM.md`, `bois/grading/BRAND_SCORING_SYSTEM.md`, `bois/rag/RAG_PIPELINE.md`, `bois/retrieval/RETRIEVAL_RULES.md`, `bois/orchestration/TRIGGER_LOGIC.md`, `bois/workflows/EXECUTION_FLOW.md`, `bois/validation/VALIDATION_GATES.md`, `bois/quality_assurance/QA_PROTOCOL.md`, `bois/monitoring/MONITORING.md`, `bois/prompts/master_orchestration_prompt.md`, `bois/presentation_systems/PRESENTATION_ENGINE.md`.

**Existing gap-backlog finding** (`00_Workspace_Intelligence_Inventory/`, generated 2026-06-03 from the prior automated diagnostic pass, baseline of 126 files / 975,639 extracted words): 29 of the diagnostic's tracked concerns are present, 5 partially present, 4 missing. **Missing** (Medium-High severity, "no direct source evidence detected"): Historical Memory, Learning Systems, Resources, Strategic Planning. **Partially present**: Constitution (High severity — fragmented, no single authoritative source), Security, Change Management, Recovery, Evolution. This inventory itself recommends a 19-folder canonical restructure that was never built — it's a diagnostic, not built content.

## 11. RACI / Ownership

| Function | Responsible | Accountable | Consulted | Informed |
|---|---|---|---|---|
| *(placeholder — but the 20-agent roster in §5 already defines purpose/memory-access/validation per role, and `00_Workspace_Intelligence_Inventory/` recommends a "Brand OS Owner / Trust and Narrative Lead" role — both ready to map against once a real owner is named)* | | | | |

## 12. Triggers / Automation Hooks

`core/orchestration/registry.py`'s `TRIGGER_RULES` is **real, implemented automation** — concrete sector/deliverable/geography/audience-keyed rules (named examples in code: fintech, healthcare, luxury, laundry, b2b) that route which of the 20 agents activate beyond the always-on 7-agent base sequence. The retrieval gate (§7, §9) is also live automation: it hard-fails rather than silently degrading on incomplete evidence. Like Finance, this is implemented automation, not a wishlist — but it has never run against real client data, only the hypothetical smoke-test client. Map into `00_Agency_Governance/AUTOMATION_APPROVAL_MATRIX.md` once real clients flow through it.

## 13. Existing OS Sub-Layer

**Yes — code-based, and more mature than originally assessed (corrected 2026-06-30, same pattern as Finance).** `12_Branding/bois/` ("Brand Operating Intelligence System") is a Python application: 44 subfolders total. **20 have real content, 24 are empty scaffolds.** `core/` contains **21 `.py` files, all confirmed real working logic — none are stubs**, a stronger ratio than Finance's finos-plugin (which had one confirmed stub out of several engines). Spot-checked modules include real TF-IDF-style retrieval scoring, a 15-dimension weighted scoring engine, real `.docx` XML parsing with SHA-256 checksums, a real knowledge-graph builder with regex entity extraction, and a hard-gated retrieval system that raises a runtime error rather than degrade silently on incomplete evidence. Its sibling `branding-os/` (a same-concept scaffold in a different naming/language convention) was confirmed dead — only a README, 18 fully empty subfolders — and was **deleted** during this restructuring's earlier cleanup phase.

**24 empty scaffold folders** (no content): `audience_intelligence/`, `brand_calendars/`, `compliance/`, `cultural_intelligence/`, `distribution_intelligence/`, `evolution/`, `identity_engineering/`, `legal_branding/`, `marketing_alignment/`, `narrative_engineering/`, `platform_adaptation/`, `production_systems/`, `psychographics/`, `sales_alignment/`, `scoring/`, `sector_intelligence/`, `semantic_systems/`, `symbolic_systems/`, `trend_intelligence/`. Many of these names map directly to rich raw-draft content (Psychographics → Draft 27, Cultural Intelligence → Draft 24, Sector Intelligence → Draft 19, Legal Branding → Draft 29) — the concepts exist in source material and in the 20-agent registry's definitions, but no dedicated module implements them; that logic currently lives only as agent metadata in `core/orchestration/registry.py`.

**Bois-to-raw-draft lineage confirmed — direct and mechanical, not inferred.** `core/ingestion/pipeline.py` was actually run (`core/executions/bootstrap_bois.py`) against this department's 40 raw drafts, producing `bois/knowledge/_indexes/documents.json` (all 40 files by exact filename, with document IDs, paragraph counts, checksums) and `paragraphs.jsonl` (14,109 tagged paragraph records, zero ingestion errors). This is a stronger citation system than Finance's hand-curated `source-intelligence.md` — auto-generated and reproducible by rerunning the script, rather than manually maintained. Beyond literal ingestion, conceptual lineage is also visible: Draft 9's governance audit domains almost exactly match `bois/governance/GOVERNANCE_SYSTEM.md`; Draft 39 (a Claude/Codex dual-agent meta-architecture draft) closely prefigures the actual orchestrator/registry/trigger-rule design; Draft 24's Cultural Alignment Score is the clearest conceptual ancestor of `core/grading/engine.py`'s 15-dimension scoring system.

**`bois/agents/` assessment**: contains one file, `AGENT_SYSTEM.md` — mature as a specification (20 fully fleshed-out agent contracts, consistent with code). **The caveat that stood here ("no executable agent runtime beyond task-building/routing; the actual reasoning step is not implemented anywhere in this codebase") was resolved 2026-07-15** — see §5 and §8. `core/synthesis/engine.py` now implements it.

**`core/` is now 24 files across 8 packages** (was 21 across 7): `core/synthesis/` added, containing `BrandAgentRuntime`. `bois/requirements.txt` added (`anthropic>=0.69.0`) — BOIS' **only** dependency, needed **only** by synthesis. Everything else remains stdlib-only, which is why routing, retrieval, governance, and scoring still run with no key and no install — the same property that keeps `smoke_test.py` and `run_arika_agency_brand.py` working unchanged.

**How `arika-runtime` reaches BOIS**: `arika-runtime/src/wrappers/bois.ts` spawns `python bois/executions/run_brand_task.py` with a JSON request and parses one JSON object back. A TS process cannot import Python in-process, and the LLM call deliberately lives in Python (§8) — so the runtime delegates rather than reimplements, exactly as `wrappers/finos.ts` dynamic-imports finos' built `dist/`. `BOIS_PYTHON` overrides the interpreter if it isn't on PATH.

**Decision (2026-06-30, reaffirmed):** `bois/` stays a grandfathered code-backed exception to the markdown-first standard — and given this migration found it's the most functionally real code layer of the two grandfathered exceptions (more so than finos-plugin), that decision looks stronger in hindsight. `bois/documentation/` and its 11 sibling spec docs (§10) remain the canonical spec; code is the implementation.

## 14. Raw Archive Pointer

~40 root-level "Draft N.md" files, 16 sampled in full for the 2026-06-30 content migration (Drafts 1, 2, 3, 5, 6, 9, 11, 14, 19, 21, 23, 24, 25, 27, 29, 32, 36, 38, 39, 40). Existing gap backlog: `12_Branding/00_Workspace_Intelligence_Inventory/` (3 files, generated 2026-06-03 — see §10 for findings).

**Low-value/generic files flagged** (same pattern seen in every other department's raw drafts):
- **Draft 40** (System Architecture Enhancement) — entirely generic "Domain Intelligence Operating System" AI-agent meta-content, **no branding-specific content at all**. Closest match yet to the low-value pattern (Sector Draft 13, Client Success Drafts 21-22, ClientPartner Drafts 8/12) — should be treated as informing bois's general architecture-thinking only, not as branding doctrine.
- **Draft 36** (Branding OS Framework) — first ~55% is near-verbatim generic "Claude Coworker repository architecture" boilerplate (constitution/governance/operations/agents/projects/context/memory folder-structure advice), but the back half pivots to a genuinely branding-specific 12-layer "BrandingOS" architecture with real content — don't discard the whole file, only the front half.
- **Draft 39** (Agentic Branding Orchestration Architect) — opens as a generic Claude-Code/Codex system-prompt-engineering exercise, but is branding-flavored throughout (positioning/narrative/voice-tone agents, brand drift detection) and is a confirmed real conceptual ancestor of bois's orchestrator design — has migration value despite the generic framing.
- **Draft 38** (Brand Revenue OS Design) — borderline: genuinely branding/revenue-specific content (tiered offers, pricing psychology, funnel mapping), but heavy on hypothetical/illustrative numbers (e.g. "$2M-$10M" revenue tiers) that must not be presented as real agency pricing.

## 15. Changelog

- 2026-06-30 — File created as part of v0.1 skeleton restructuring; dead `branding-os/` duplicate and `bois/` `__pycache__` directories removed in cleanup.
- 2026-06-30 — Content migration: `bois/documentation/` + 11 sibling spec docs read in full, all 21 `core/` Python files spot-checked, and 16 of 40 raw drafts sampled. **Corrected** the original "partially-real" characterization — confirmed all 21 `core/` files are real working logic (none stub), confirmed a real auto-generated source-citation system (40 docs / 14,109 tagged paragraphs, actually run via `bootstrap_bois.py`), and confirmed direct mechanical lineage from raw drafts to code architecture. Populated Capability Registry, Workflow Index, Agent Roster (20 agents), KPI Dictionary, Standards & SOPs Index, and Triggers/Automation Hooks. Flagged 4 low-value/generic raw drafts. All scoring thresholds and brand examples confirmed generic/illustrative, no real agency brand data found anywhere. — Claude Code (Sonnet 4.6)
- 2026-06-30 — Confirmed by direct code inspection (no LLM imports/calls anywhere in `bois/`, matching Finance's equivalent check) and resolved 2 tracker items: (28) AI agents stay spec/routing-only until the agency's own real brand exists (item 30) to give them something real to reason over; (29) `BrandScoringEngine`'s generic 70-78 thresholds stay as-is until a real BOIS run exists to calibrate against. — Claude Code (Sonnet 4.6)
- 2026-06-30 — **Resolved tracker item 30.** Built and ran `bois/executions/run_arika_agency_brand.py`, assembling a real `ClientObject` for Arika Agency itself entirely from facts already confirmed elsewhere in this repo (no invention) and running it through the full pipeline (`create_client_workspace` → memory save → gated retrieval → agent routing → context persistence). Confirmed run: retrieval gate complete (zero missing sources), 11 of 20 agents activated, real workspace written to `bois/clients/arika-agency/`. `competitors`, `visual_preferences`, `typography_preferences`, `color_preferences` left empty — genuinely unconfirmed anywhere in the repo, flagged rather than invented. BOIS now has its first non-hypothetical client object alongside the "Sample Nairobi Laundry" smoke test. — Claude Code (Sonnet 4.6)
- 2026-06-30 — **Item 28 superseded the same day item 30 resolved.** With item 30's precondition closed (a real brand for the agents to reason over now exists), owner confirmed **Claude (Anthropic API)** as the LLM to wire into the 20-agent roster. Not built yet — no Anthropic SDK dependency added to `bois/` in this entry; added to `00_Agency_Governance/GO_LIVE_CHECKLIST.md`. — Claude Code (Sonnet 4.6)
- 2026-07-01 — Added §16 Memory/Feedback Loop/Cadence per the go-live plan in `00_Agency_Governance/GO_LIVE_CHECKLIST.md`. — Claude Code (Sonnet 5)
- 2026-07-01 — **Partially resolved the visual/typography/color gap from tracker item 30.** Owner supplied moodboard imagery during the new Design (19) department's founding planning session, giving `visual_preferences`/`typography_preferences`/`color_preferences` a real starting direction (palette and typeface names, aesthetic direction) for the first time — captured in full at `19_Design/DESIGN_INSPIRATION.md` and summarized in §2. Exact hex/weight specs and `competitors` remain genuinely open — not invented. New Design department (19) now owns production execution against this direction; Branding retains ownership of the identity/strategy layer itself. — Claude Code (Sonnet 5)
- 2026-07-03 — **Real hex/typography values confirmed via the "Brand Genome"** (owner-run Gemini session, reviewed and adopted) — supersedes the 2026-07-01 direction-only entry. Full values in §2/§8. Flagged transparently as AI-generated creative direction (Gemini), owner-confirmed, not a human designer's sign-off — same sourcing honesty applied to Claude's own earlier (rejected) draft. Source material was cut off mid-paste; may need revisiting if more arrives. Logo pixel specs and `competitors` still open. — Claude Code (Sonnet 5)
- 2026-07-03 — **Full brand kit resolved** — owner explicitly confirmed the final logo, closing out colors + typography + logo entirely. Only `competitors` remains open (split into its own tracker item, 47). — Claude Code (Sonnet 5)
- 2026-07-03 — **Completed the Brand Genome capture** — owner supplied the rest of the previously-truncated paste (exact color percentages, typography styling rules, graphic asset toolkit, art direction, composition rules). All now in §2. Confirmed no canonical logo file exists yet, which blocks real logo lockup specs — being addressed as a Design (19) task. — Claude Code (Sonnet 5)
- 2026-07-03 — **Logo finalized.** Generated 4 real logo candidates in Canva; owner selected and lightly edited one (`https://www.canva.com/d/Q9Qmq2wvwj6utVB`), confirming **The Seasons** and **The Youngest** (both verified real, Canva-available fonts) as the logo's own wordmark typefaces. Proposed a standard lockup spec (exclusion zone, minimum size, construction grid) against the real file — pending owner sign-off on the specific numbers.
- 2026-07-03 — **Logo explicitly confirmed final by owner** ("the logo is okay") — lockup spec accepted by extension. Full brand kit (colors, typography, logo) now completely resolved; only `competitors` remains open (item 47). — Claude Code (Sonnet 5)
- 2026-07-15 — **BOIS WIRED — the department can think.** Diagnosis first: `bois` was the only code layer in the repo that could not reason. `EXECUTION_STAGES` listed **`output_synthesis` as stage 4 of 7 with no implementation** — `agent_prompt()` built a prompt nothing consumed (`registry.py:157`, zero callers), `AgentResult` was a dataclass nothing produced (`models.py:128`, zero producers). BOIS could route, retrieve, score, validate, and remember; the step that produces the output was simply absent, so governance and memory were finished and waiting on an input that never arrived. Built `core/synthesis/engine.py` (`BrandAgentRuntime`, mirroring finos' `ClaudeAgentRuntime`), `requirements.txt` (`anthropic>=0.69.0` — the only dependency, needed only by synthesis), `BrandCognitiveOrchestrator.synthesize()` (closing agent_activation → output_synthesis → governance_validation and persisting to the governance/client memory streams), and `executions/run_brand_task.py` (JSON in/out, with `synthesize:false` as a first-class no-key mode). **Cured a latent contract bug that would have failed governance on every run forever** — three divergent output lists meant `emotional_reasoning`, `visual_reasoning`, and `positioning_rationale` were required by governance but never requested by any prompt; now one imported `AGENT_OUTPUT_CONTRACT` (§8). **Closed the stale `client.json`** — Arika's real client object still had empty visual/typography/color fields three days after the Brand Genome was adopted; populated from confirmed values and re-ran (`competitors` left open, item 47). Runtime side: `bois_mode` added to the spec schema (+ a regression test), `src/wrappers/bois.ts` delegates via subprocess, executor case added, and **3 agents exposed** (`branding-brand-definition/-brand-audit/-governance-validator`) rather than 20 — BOIS' agents are a routed chain, not independent listeners, so the runtime invokes the orchestra and BOIS conducts. **Verified end-to-end with no API key**: TS → subprocess → Python → 11 agents routed, retrieval gate complete, memory written; `synthesize:true` without a key degrades gracefully rather than crashing. `arika list` → 64; `npm test` → 9/9. — Claude Code (Opus 4.8)

## 16. Memory / Feedback Loop / Cadence

**Memory**: Already real and implemented in code — `core/memory/store.py`'s append-only JSONL event log across 8 memory streams (client/campaign/visual/narrative/governance/market/trend/stakeholder, §3). This is the most mature Memory layer of any department (code-enforced persistence, not an empty template) — but has only been exercised against the Arika Agency real-client run and the "Sample Nairobi Laundry" smoke test (§2), not live client work yet.

**Feedback Loop**: Brand scoring/audit (§4, §7 — 15 weighted dimensions, thresholds 70-78) flags failed dimensions with attached recommendations; Governance validation (§4) separately checks retrieval completeness, agent activation, and 8 mandatory reasoning fields. Both are real, implemented checks (`core/grading/engine.py`, `core/governance/policy.py`).

**✅ The loop is closed as of 2026-07-15.** The open question here — *"should write their outcome back into the memory store's governance/market streams — confirm this wiring exists when Phase 2 Claude-wiring work touches `bois/`"* — was the right question, and the answer was **no, it didn't exist**, because nothing produced an output to write back. `BrandCognitiveOrchestrator.synthesize()` now writes every agent's reasoning to the **`governance`** stream (`agent_output_synthesized`: status, confidence, validation notes) and each agent's own `memory_updates` to the **`client`** stream (`agent_memory_update`) — which is what `memory_updates` was always for and why it survives in the cured 10-field contract (§8). Separately, `arika-runtime` writes its own uniform envelope to `12_Branding/_memory/runtime.jsonl` on every run, so Branding is now visible to `operations-state-monitor` (08) like every other department. **Two memory layers, deliberately**: BOIS' 8 domain streams (brand cognition) and the runtime's JSONL (execution record).

**Still true**: `operations-state-monitor` (08) carries "Branding's `bois` has code but no LLM wiring (it can't think)" as a known structural fact. **That fact is now stale** — see §5. Nine agents across five departments defer to Branding for brand truth (`clientpartner-partner-enablement`, `content-narrative-architect`, `design-brand-environment-consistency-checker`, five Experience Engineering agents); as of 2026-07-15 that authority can answer.

**Cadence**: Event-triggered, not calendar-fixed — brand scoring runs "per-deliverable/audit" (§7). Maps loosely to the Opportunity Calendar (per-client/per-brief) rather than a fixed daily/weekly rhythm; the Brand Evolution Agent (§5) is the closest analog to a Strategic Calendar function.
